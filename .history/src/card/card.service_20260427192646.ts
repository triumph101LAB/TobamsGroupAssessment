import { Injectable } from "@nestjs/common";

@Injectable()
export class CardService{

    validatecard(cardnumber:string): string {
        cleanedString = 
        cardnumber.replace()
 
        return "tr"
    }
    
}